import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // 你可以自定義主色
    },
    secondary: {
      main: "#dc004e",
    },
  },
});

export default theme;
